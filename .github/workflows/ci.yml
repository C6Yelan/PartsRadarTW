name: CI

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  quality:
    name: Quality
    runs-on: ubuntu-latest
    timeout-minutes: 20
    env:
      DATABASE_URL: postgresql://ci:ci@127.0.0.1:5432/partsradar_ci
      PARTSRADAR_SKIP_DOTENV: "1"
    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Set up pnpm
        uses: pnpm/action-setup@v6
        with:
          version: 11.3.0

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Prepare pinned cloudflared image
        run: docker pull cloudflare/cloudflared:2026.7.2@sha256:4f6655284ab3d252b7f28fedb19fe6c8fc82ee5b1295c20ac74d475e5398a52d

      - name: Generate Prisma client
        run: pnpm db:generate

      - name: Validate Prisma schema
        run: pnpm db:validate

      - name: Typecheck
        run: pnpm typecheck

      - name: Lint
        run: pnpm lint

      - name: Run non-integration tests
        env:
          PARTSRADAR_REQUIRE_CLOUDFLARED_CONTAINER_TEST: "1"
        run: pnpm test

      - name: Build web
        run: pnpm build:web

  db-integration:
    name: PostgreSQL integration
    runs-on: ubuntu-latest
    timeout-minutes: 15
    services:
      postgres:
        image: postgres:18
        env:
          POSTGRES_DB: partsradar_test
          POSTGRES_PASSWORD: partsradar_admin
          POSTGRES_USER: partsradar_admin
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U partsradar_admin -d partsradar_test"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10
    env:
      DATABASE_URL: postgresql://partsradar_test:partsradar_runtime@127.0.0.1:5432/partsradar_test
      MIGRATION_DATABASE_URL: postgresql://partsradar_admin:partsradar_admin@127.0.0.1:5432/partsradar_test
      PARTSRADAR_SKIP_DOTENV: "1"
      PARTSRADAR_TEST_DATABASE_ISOLATED: "1"
      POSTGRES_RUNTIME_PASSWORD: partsradar_runtime
      POSTGRES_RUNTIME_USER: partsradar_test
      TEST_DATABASE_URL: postgresql://partsradar_test:partsradar_runtime@127.0.0.1:5432/partsradar_test
    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Set up pnpm
        uses: pnpm/action-setup@v6
        with:
          version: 11.3.0

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Generate Prisma client
        run: pnpm db:generate

      - name: Deploy migrations
        run: pnpm db:deploy

      - name: Configure runtime database role
        run: pnpm db:configure-runtime-role

      - name: Run PostgreSQL integration tests
        run: pnpm test:db

  e2e-smoke:
    name: Playwright browser gates
    runs-on: ubuntu-latest
    timeout-minutes: 20
    services:
      postgres:
        image: postgres:18
        env:
          POSTGRES_DB: partsradar_e2e
          POSTGRES_PASSWORD: partsradar_admin
          POSTGRES_USER: partsradar_admin
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U partsradar_admin -d partsradar_e2e"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10
    env:
      DATABASE_URL: postgresql://partsradar_e2e:partsradar_runtime@127.0.0.1:5432/partsradar_e2e
      E2E_SKIP_BUILD: "1"
      MIGRATION_DATABASE_URL: postgresql://partsradar_admin:partsradar_admin@127.0.0.1:5432/partsradar_e2e
      PARTSRADAR_SKIP_DOTENV: "1"
      PARTSRADAR_TEST_DATABASE_ISOLATED: "1"
      POSTGRES_RUNTIME_PASSWORD: partsradar_runtime
      POSTGRES_RUNTIME_USER: partsradar_e2e
    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Set up pnpm
        uses: pnpm/action-setup@v6
        with:
          version: 11.3.0

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Install Chromium
        run: pnpm exec playwright install --with-deps chromium

      - name: Generate Prisma client
        run: pnpm db:generate

      - name: Deploy migrations
        run: pnpm db:deploy

      - name: Configure runtime database role
        run: pnpm db:configure-runtime-role

      - name: Build web
        run: pnpm build:web

      - name: Run mocked and state browser tests
        run: pnpm e2e:mocked

      - name: Run deterministic DB-backed smoke
        run: pnpm e2e:db-smoke
